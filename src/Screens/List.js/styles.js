import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex: 1,
    },
    viewLogo:{
        alignSelf: 'center',
        marginTop: 20,
    }
})

export default styles